vim /etc/nginx/sites-available/default
 -> add or remove lister for port 80

 ssh <user>@<local ip>

 deploy.sh runs script to build and export code to server
 - may have to open git BASH terminal

 npm run dev

